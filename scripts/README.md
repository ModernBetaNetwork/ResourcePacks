# Resource pack scripts

You need to have [Bun](https://bun.sh) installed to run the scripts in this repo.

```sh
cd scripts
bun install
cd ..
./scripts/build.ts
# ...or...
bun run ./scripts/build.ts
```
